import streamlit as st
import FinanceDataReader as fdr
import pandas as pd
import plotly.express as px

# 페이지 설정을 가장 먼저! (화면 넓게 쓰기)
st.set_page_config(page_title="내 주식 현황판", layout="wide")
st.title("🚀 내 포트폴리오 실시간 맵 (섹터별)")

# ---------------------------------------------------------
# ▼▼ 내 포트폴리오 데이터 (섹터 추가 완료) ▼▼
# ---------------------------------------------------------
my_portfolio = {
    # 1. 섹터 정보를 추가했습니다. (마음에 안 들면 수정 가능!)
    '섹터': [
        '반도체/IT', '반도체/IT', '방산/기계', '금융지주', '방산/기계', 
        '자동차', '음식료', '방산/기계', '금융지주', '전력/인프라', 
        '금융지주', '음식료', '금융지주', '가전/IT', '전력/인프라', 
        '조선/중공업', '금융지주',
        '미국 빅테크', '미국 지수ETF', '미국 지수ETF', '미국 전기차', 
        '미국 금융', '미국 빅테크', '미국 반도체'
    ],
    '종목명': [
        '삼성전자', 'SK하이닉스', 'LIG넥스원', '하나금융지주', '현대로템', 
        '현대차', '오리온', '한화', 'LG', 'TIGER AI전력기기', 
        'WON 초대형IB', 'KT&G', 'KB금융', 'LG전자', '효성중공업', 
        'HD현대중공업', 'KODEX 주주환원',
        'Alphabet C', 'Invesco QQQ', 'TQQQ', 'Tesla', 
        'Berkshire B', 'Zeta Global', 'Qualcomm'
    ],
    '종목코드': [
        '005930', '000660', '079550', '086790', '064350', 
        '005380', '271560', '000880', '003550', '0117V0', 
        '453470', '033780', '105560', '066570', '298040', 
        '329180', '476650',
        'GOOG', 'QQQ', 'TQQQ', 'TSLA', 
        'BRK-B', 'ZETA', 'QCOM'
    ],
    '수량': [
        151, 12, 39, 114, 20, 
        27, 32, 24, 90, 500,
        1100, 80, 21, 25, 2,
        17, 800,
        17, 2, 3, 4, 
        2, 58, 4
    ],
    '매수단가': [
        117639, 736000, 523833, 98789, 196918, 
        388518, 115500, 125000, 88428, 14450, 
        10350, 147500, 132605, 106700, 2208000, 
        615235, 10430,
        287.55, 624.58, 54.50, 466.97, 
        493.98, 23.52, 182.39
    ]
}

@st.cache_data
def load_data():
    df = pd.DataFrame(my_portfolio)
    current_prices = []
    exchange_rate = 1450 

    for code in df['종목코드']:
        try:
            stock_data = fdr.DataReader(code)
            price = stock_data['Close'].iloc[-1]
            if not code.isdigit() and not code.endswith('0'): 
                price = price * exchange_rate
            if code in ['0117V0']:
                 price = price
            current_prices.append(price)
        except Exception as e:
            print(f"Error: {e}")
            current_prices.append(0)
    
    df['현재가'] = current_prices
    df['평가금액'] = df['현재가'] * df['수량']
    df['매수단가_원화'] = df.apply(lambda x: x['매수단가'] * exchange_rate if (not str(x['종목코드']).isdigit() and x['종목코드'] not in ['0117V0']) else x['매수단가'], axis=1)
    df['수익률(%)'] = ((df['현재가'] - df['매수단가_원화']) / df['매수단가_원화']) * 100
    
    return df

if st.button('🔄 시세 새로고침'):
    st.cache_data.clear()

try:
    df_result = load_data()
    
    total_asset = df_result['평가금액'].sum()
    st.metric(label="💰 내 총 자산 (추정)", value=f"{total_asset:,.0f} 원")

    # ▼▼▼ 트리맵 설정 변경 핵심 부분 ▼▼▼
    fig = px.treemap(
        df_result, 
        path=['섹터', '종목명'], # 2. 섹터 -> 종목명 순으로 계층핑
        values='평가금액', 
        color='수익률(%)',
        # 3. 색상 범위 설정 (±3%에서 최대 색상 고정)
        color_continuous_scale=['#FF0000', '#F0F2F6', '#00FF00'], # 더 진한 빨강/초록 사용
        color_continuous_midpoint=0,
        range_color=[-3, 3], # 이 범위를 벗어나면 최대 색상으로 고정됨
        height=900 # 1. 화면 높이 키우기 (기본값보다 훨씬 크게)
    )
    # ▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲▲

    fig.data[0].textinfo = 'label+text+value'
    fig.data[0].texttemplate = "%{label}<br>%{customdata[0]:.2f}%"
    fig.data[0].customdata = df_result[['수익률(%)']]
    
    # 글자 크기도 조금 키움
    fig.update_layout(font=dict(size=16))

    st.plotly_chart(fig, use_container_width=True)
    
    with st.expander("📊 표로 상세 보기 (클릭해서 열기)"):
        st.dataframe(
            df_result[['섹터', '종목명', '수량', '현재가', '수익률(%)', '평가금액']].style.format({
                '현재가': '{:,.0f}', 
                '평가금액': '{:,.0f}', 
                '수익률(%)': '{:.2f}%'
            })
        )
    
except Exception as e:
    st.error(f"오류가 발생했습니다: {e}")
